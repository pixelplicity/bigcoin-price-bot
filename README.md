# BIGCOIN PRICE DISCORD BOT

Bot that updates the BIG price and blcosk to halvening every minute

## Running

Build the script (stored in `/dist`)

`pnpm build`

Run the built script use=ing

`pnpm start`

Run the typescript directly using ts-node with:

`pnpm dev`
